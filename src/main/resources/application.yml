spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cardealership
    username: user
    password: user

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  liquibase:
    enabled: false